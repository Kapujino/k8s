---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nfs-client-provisioner
  name: nfs-client-provisioner
spec:
  template:
    spec:
      containers:
        - name: nfs-client-provisioner
          env:
            - name: NFS_SERVER
              value: 192.168.1.10
            - name: NFS_PATH
              value: /mnt/volumes/nfs/dynamic
      volumes:
        - name: nfs-client-root
          nfs:
            server: 192.168.1.10
            path: /mnt/volumes/nfs/dynamic
