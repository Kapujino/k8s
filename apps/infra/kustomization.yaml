apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd

resources:
  - calico.yaml
  - metallb.yaml
  - argocd.yaml
  - traefik.yaml

commonLabels:
  app.kubernetes.io/app-type: infra
